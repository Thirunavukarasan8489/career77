# Model-to-Screen Mapping

*   **Candidate Applications Page** -> `/api/applications` -> Application model -> Job model -> Candidate model.
*   **Candidate Saved Jobs Page** -> `/api/candidates/saved-jobs` -> Candidate model (populates saved jobs).
