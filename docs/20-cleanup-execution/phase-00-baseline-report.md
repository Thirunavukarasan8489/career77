# Career77 — Phase 00 Baseline Report

## 1. Git Status
*   On branch `checking`
*   Modified files inside `app/(candidate)/candidate/jobs/page.tsx`, etc.
*   Untracked files inside `app/(candidate)/candidate/saved/`, etc.

## 2. Project Structure
Standard structures followed (`app/`, `components/`, `lib/`, `models/`, `context/`).

## 3. Package Versions
*   Next.js: `16.2.10`
*   React: `19.2.4`
*   Mongoose: `^9.7.4`
*   NextAuth: `^4.24.14`

## 4. Available Scripts
*   `dev`: `next dev`
*   `build`: `next build`
*   `start`: `next start`
*   `lint`: `eslint`
*   `seed`: `tsx scripts/seed.ts`

## 5. Lint Result
*   Failed. ESLint threw 132 errors and 75 warnings, primarily from React 19 rules (e.g. `react-hooks/set-state-in-effect`).

## 6. TypeScript Result
*   Passed (Finished check successfully in compilation check).

## 7. Build Result
*   Passed (Next.js build runs and optimizes static pages successfully).

## 8. Test Result
*   Not applicable (No project-specific script exists).

## 9. Application Startup
*   Development environment starts successfully via `npm run dev`.

## 10. Route Baseline
Recorded.

## 11. API Baseline
Recorded.

## 12. Database Model Baseline
Recorded.

## 13. Component Baseline
Recorded.

## 14. Environment Configuration Status
MongoDB URI parameter is present.

## 15. Existing Errors
Linter warning issues inside older layout code blocks.

## 16. Existing Warnings
Linter warnings.

## 17. Known Broken Areas
None.

## 18. Known Risks
None.
